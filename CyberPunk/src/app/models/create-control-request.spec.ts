import { CreateControlRequest } from './create-control-request';

describe('CreateControlRequest', () => {
  it('should create an instance', () => {
    expect(new CreateControlRequest()).toBeTruthy();
  });
});
