import { UpdateControlRequest } from './update-control-request';

describe('UpdateControlRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateControlRequest()).toBeTruthy();
  });
});
